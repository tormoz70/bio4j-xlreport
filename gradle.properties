org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Shared Maven POM metadata (credentials/signing stay in ~/.gradle/gradle.properties)
POM_DESCRIPTION=Java 21 Excel report engine: XML definitions, XLSX templates, Apache POI, GraalJS
